# At least one provider key is required. OPENAI_API_KEY is also used as the
# answer parser, so it's required regardless of which models you query.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
PERPLEXITY_API_KEY=
GEMINI_API_KEY=
